name: Auto PR from prod to pages
on:
  workflow_run:
    workflows: ["Production Branch Workflow"]
    types:
      - completed
    branches:
      - prod
jobs:
  create-pull-request:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Create pull request
        uses: actions/github-script@v7
        with:
          script: |
            const { data: existingPullRequests } = await github.pulls.list({
              owner: context.repo.owner,
              repo: context.repo.repo,
              head: 'prod',
              base: 'pages',
              state: 'open'
            });
            if (existingPullRequests.length === 0) {
              await github.pulls.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                head: 'prod',
                base: 'pages',
                title: 'Auto PR: Synchronize prod with pages',
                body: 'This pull request was automatically created to merge prod into pages after successful workflow run.'
              });
            } else {
              console.log('An open pull request already exists for prod -> pages.');
            }
